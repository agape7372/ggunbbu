# 건뿌 전면 업그레이드 로드맵 (2026-08-30)

정본. 조사 3종(코드 감사 · 실플레이 QA 정량 계측 · 재사용 자원 인벤토리) 합치 결과에 기반.
목표선: **Play 스토어 출시까지** (사용자 확정). 아트: **AI 생성 PNG 파이프라인** (사용자 확정).

## 진단 요약

"완전 개판"의 실체 = 08-29 「점프 상향+층 밀착」 커밋(8a9da28)이 기존 5개 시스템과 충돌:

| 증상 (실측) | 원인 |
|---|---|
| 지면에 건물 있으면 점프가 13px 홉으로 소멸 | `clingToFloors`가 낙하/정지 스택 밑면에 접착 (`p.vy <= stack.vy`) |
| 성공한 점프는 체공 2.77s 중 75.9% 지면이 화면 밖 | JUMP_V0 1600(정점 719px > 필드 470px) + 카메라 무보정 1:1 추종 |
| 공중에서 번개 예고·대포 신호 소실 | 예고 마커가 카메라 변환 안쪽에서 그려짐 |
| 화산탄 페이즈에서 점프=연쇄 피격 | 지면 착지 전부를 hurtPlayer 직행 처벌 (정본은 「더미 위 착지=깔림」) |
| 테스트 93 green인데 위 전부 통과 | cling.test가 버그를 정답 고정, 봇이 점프 안 함, 히트스톱이 렌더 전용 |

코어 시뮬 순수성·체크포인트 산술·전단지 톤·조작 배치는 건강 — 보존한다.

## Wave 0 — 응급 수술 (게임을 다시 플레이 가능하게)

1. ~~**cling 삭제**~~ **← 08-30 오후 사용자가 뒤집음. 아래 "밑면 접촉" 절이 현행 계약이다.**
   (당시 판정) cling 삭제 (재설계 아님). 8a9da28 이전 known-good 복원: 점프=제자리 회피+공중 무적, 시각 관통 허용. `p.cling` 필드·`clingToFloors`·`tests/cling.test.ts` 제거.
2. **JUMP_V0 1600 → 1150** (정점 ≈371px, 필드 470 안). ★08-29 사용자 지시("훨씬 올리고")를 실측 근거로 되돌림 — 그 지시의 판정은 이미 깨진 카메라를 통해 내려진 것. 이 기록 없으면 다음 세션이 1600 재복원 시도한다. 최종 판정은 사용자 실플레이.
3. **카메라 고정(cam=0)**. 정점이 필드 안이므로 추종 불필요. camera.ts는 클램프 구조만 남김.
4. **예고 마커 화면공간 분리**: 번개 큐·대포를 카메라 변환 밖에서 드로우.
5. **화산탄 착지 = PINNED 라우팅**: `sim.ts:81` hurtPlayer 직행 제거, 더미 상면 접촉 시 기존 깔림 상태기계(탈출 3분기)로. **바닥 파밍(점수+게이지 반복 획득)은 정본 — 보존.**
6. **히트스톱 core 이동**: sim이 이벤트 발행 시점에 JUICE 테이블로 hitstop 주입, renderer의 상태 변이(`renderer.ts:63`) 제거. 시간축 단언 재보정(타격 수 공간 30/138/12/48은 불변).
7. **타이틀 메뉴 입력 엣지화**: 가드 홀드 60Hz 회전 제거.
8. **웹 경제 봉인**: platform 스텁이 웹에서 `unavailable` 반환(`?debug=1` 제외). IAP 섹션 숨김, 광고 부활은 웹에서 광고 문구 제거("일어나기"), 광고 궤도 버튼 "앱에서만".
9. **ATTACK_REACH 정리**: act2.ts 하드코딩 128을 config `ACT2_REACH`로 승격 + 출처 주석(계획서 스펙 128 vs 원작대조 튜닝 68 병존 기록). 수치 변경 없음.

**종료 기준**: 각 항목에 구코드에서 빨간 회귀 테스트 + 브라우저 재계측 2숫자(점프 소멸 없음 / 체공 중 지면 소실 ~0%) + `npm test`·`tsc` green + 배포.

## Wave 1 — 손맛·연출 (JUICE 매트릭스 완성)

- 이펙트 좌표계 수리: 월드 좌표 통일(카메라 굽기 제거), rAF 종속 → 고정 스텝 적분(120Hz 2배속 버그), 필드 클립 레이어링.
- 계획서 JUICE 매트릭스 완주: trauma 셰이크·스쿼시&스트레치·의성어 에스컬레이션·콤보 카운터 juice·게이지 피드백·낙하 그림자(게임플레이 정보).
- 필살 3종 재설계: ageba `break` 버그(HP1만 파괴 → 관통 로직), tetsu/tenchi 스택 부재 시 게이지 보존 or 대체 효과, tetsu 장착 시 화산탄 제거 수단 확보.
- 조용한 무반응 제거: 상점 잔액부족(미션 탭 disabled+0.42 패턴 복제), 필살 게이지 부족(경고음+흔들림).
- 무적 점멸 렌더 수정, 방치 사망 템포 재검(PIN_REPEAT 120f).

## Wave 2 — 메타 실배선 + 진행 곡선

- 기록 오염 차단: 구역작전/디버그 판은 bestArcade·unlockedChapters에 미반영(출처 플래그).
- pause→탭바 desync 수정, 판 도중 먼지 소실 경로 봉합(grant 시 saveSave).
- 코스메틱 실구현: loadout.body/blade/letters를 프로시저럴 렌더 파라미터에 실배선 — 팔리는 물건이 실제로 바뀌게.
- 마천루 sharedHp 실구현(층 공유 HP 풀 — 현재 무효 파라미터).
- 저장 마이그레이션: levain `persistence.ts` 사다리(`parse → migrate → validateAndClamp`) 이식, `storage.ts:81` 전멸 경로 제거. **앱 승격 전 필수.**
- 죽은 코드 16건 정리(api.ts·bridge.ts·LEFTOVER_IDS·이중 타입 등), waza_unlock 3회차 지급 버그.
- 1막 곡선 재보정: 해금 9,999,999 [정본] 유지, 히트스톱 반영 재실측 후 config 노브로 체감 7~9분 복원. act1curve 단언 밴드 강화.

## Wave 3 — 아트 파이프라인 (AI PNG)

- 그록/이미지봇 JSON 프롬프트(`docs/GROK_IMAGE_PROMPTS.md` 확장)로 manifest 미충족 11키 생성: char/* 9(뿌뿌 포즈 시트), fx/* 2. 브랜드 정본(전단지 병맛, 24색) 준수.
- `render-pipeline.test.ts:49` 수정(에셋 부재를 단언 중 — PNG 넣는 순간 깨짐).
- 배경 리메이크: 상단 공백 해소(하늘 채움 구성으로 재생성), 중복 제거(act2==europe-skyline, bonus==modern), 미참조 skyline 4장(4.5MB) dist 제외.
- 달 보스(렌더 300px vs 히트박스 80×64)·대성당(회색 상자) 비주얼 재작업, 건물 스큐 의도 판정.
- 검수 하네스: levain `breadlab-shot.mjs` 방식 — 캡처·판정은 서브에이전트 루프, 숫자 판정 우선.
- 에셋 예산 게이트(`check-budget.mjs` 개념 이식).

## Wave 4 — 앱 승격 (Play 내부테스트)

- 셸: `shell/` 하위 폴더(자체 package.json — 루트 런타임 의존성 0 유지). levain 뼈대 복사(capacitor.config.ts, android 3파일 — 한글경로 `overridePathCheck`, 서명 블록, SDK36). 웹→셸 브리지는 `window.Capacitor.Plugins.X` 런타임 조회(podoal 검증 패턴, 동적 bare import 금지).
- AdMob 리워드: podoal `ads.ts` 구조(무throw·닫힘 상한 타이머) + AndroidManifest APPLICATION_ID meta-data(없으면 기동 크래시). 부활·궤도 광고 실배선. SSV는 백엔드 없으므로 클라 콜백 지급(로컬 세이브 게임 — 위험 수용, 문서화).
- **IAP는 v1 제외** (사내 선례 0 + 코스메틱 실구현 후에나 상품 성립). 상점 IAP 섹션은 네이티브에서도 숨김, v2 로드맵으로.
- OTA: levain 3종(`ota.ts`+`ota-release.mjs`+`ota/` 호스팅) 이식. notifyAppReady·rmSync 함정·발행 전 git status 절차 포함. versionCode↔OTA 4곳 동기 스크립트.
- Play 콘솔: levain `STORE_LISTING.md`·`PRIVACY.md` 양식 재작성(광고 있음), keystore 신규, 내부 테스트 제출. levain `QA.md` B·D절 릴리스 게이트.

## Wave 5 — 마감

실기기 스모크(터치 4버튼 동시 입력·오프라인·설치) → DEVLOG·레포 문서·메모리·위키 현황 갱신.

## 불변 (모든 웨이브 공통)

- 체크포인트 산술 30/40/148/168/180/228/230 · core 순수성 · config 단일 출처 · 루트 런타임 의존성 0.
- 원작 에셋 미사용. 병맛은 포장 레이어만(필드 판독성 침범 금지).
- 위임: 단순작업 Sonnet5+ (Haiku 금지). core/통합/테스트는 메인 직접.


---

## ★계약 정정 — 밑면 접촉 (2026-08-30 오후, 사용자 확정)

Wave 0에서 "점프 = 제자리 회피 + 공중 무적 + 시각 관통"으로 정리했던 판정을 **사용자가 뒤집었다.**
근거는 계획 정본(원작 대조)이다:

> `공격: 건물을 약간 위로 밀어냄` · `정석: 최하층 HP1 남기고 점프가드 → 공중에서 아래층 제거 → 반복`
> `건물 완전 접지 = 라이프 1 손실 + 깔림`

즉 **부수며 건물을 띄워 버티는 것이 방어**고, **못 부수면 눌려 내려가 죽는 것이 리스크**다.
관통은 그 리스크를 통째로 지워 게임을 무르게 만든다. "공중이면 짓눌리지 않는다"도 원작이 아니다.

**현행 계약** (`src/core/player.ts` `contactWithStack`):
- 상승 중 머리가 밑면에 닿으면 **거기서 멈춘다**(통과 금지).
- 밑면이 내려와 덮으면 **같이 눌려 내려간다**. 지면까지 눌리면 접지 검사가 깔림으로 받는다.
- ★cling과의 차이: cling은 **닿지도 않았는데** `p.vy <= stack.vy`만으로 스냅해 점프가 13px 홉으로
  소멸했다. 여기서는 **실제로 겹칠 때만** 개입하고, 밑면 아래 설 자리가 없으면 좌표를 안 건드린다.
- 필살(무적·전파괴)·보너스(버터바) 판은 제외.

**가드**: `tests/wave0.test.ts`(막힘 상한·눌림·비접착·홉 소멸 방지) + `tests/combat.test.ts`
"밑면 접촉 [08-30 개정]". 121 green.

**교훈**: Wave 0의 삭제 판단은 *체감 계측*(13px 홉)으로는 옳았지만 *원작 대조*로는 틀렸다.
증상(점프가 죽음)의 원인은 "밀착" 자체가 아니라 **접촉하지 않았는데 스냅한 것**이었다.
문서에 [정본] 딱지가 붙어 있어도, 그 딱지를 누가 무엇을 보고 붙였는지 확인할 것.
